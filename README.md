# FinFlow 个人记账

## 概述
这是一个基于 Flutter 与 SQLite 的极简风格个人记账应用，支持收入与支出记录的快速录入，数据本地持久化保存，交互流程简洁流畅。

## 功能概览
- **多账本与多账户管理**：支持创建多个账本（如日常、旅行、装修）与多个账户（如微信、支付宝、银行卡），实现精细化资产管理。
- **智能记账交互**：
  - 底部弹层 + 自研数字键盘，支持加减法运算（如输入 `12+8` 自动计算为 `20`）。
  - 支持“再记一笔”功能，保存当前记录后保留界面，仅清空金额与备注，提升连续记账效率。
  - 自动记忆上次使用的分类与账户。
- **数据查询与检索**：
  - 首页按日期分组展示交易记录，并显示每日收支汇总。
  - 支持按关键字搜索备注与金额。
  - 下拉加载更多历史数据，优化长列表性能。
- **数据管理**：
  - 支持记录的编辑与长按删除。
  - 账本与账户的管理（新增、修改名称、删除），默认账本/账户受保护不可删除。
  - 账单数据导出：支持筛选日期、账本、账户导出为 Excel (.xlsx) 文件，方便二次分析。
  - 数据导入：支持从 Excel 文件导入数据（开发中/已预留入口）。
- **本地化存储**：所有数据存储在本地 SQLite 数据库中，隐私安全，无网络依赖。

## 运行环境
- Flutter 3.x
- Android、Windows（Windows 用于调试与桌面体验）

## 快速开始
1. 安装依赖
   ```bash
   flutter pub get
   ```
2. 运行应用
   ```bash
   flutter run -d windows
   ```
3. 打包 apk
   ```bash
   flutter build apk --release -v
   ```

## 项目结构
### 核心目录与文件说明
```
d:\CODE\FinFlow\
├── lib\
│   ├── data\
│   │   ├── record_database.dart      # 数据库核心层
│   │   │   - 单例模式管理 SQLite 数据库连接
│   │   │   - 处理表结构创建（records, bills, accounts）与版本升级
│   │   │   - 封装所有 CRUD 操作与数据统计接口
│   │   │   - 包含 Windows 平台的 FFI 适配逻辑
│   │   │
│   │   └── transaction_record.dart   # 数据模型定义
│   │       - TransactionRecord: 交易记录实体（金额、分类、时间、备注等）
│   │       - Bill: 账本实体（名称、是否默认）
│   │       - Account: 账户实体（名称、余额、是否默认）
│   │       - 包含 JSON/Map 序列化与反序列化方法
│   │
│   ├── ui\
│   │   ├── home_page.dart            # 主界面
│   │   │   - 包含“记账”与“管理”双 Tab 页签
│   │   │   - 记账页：展示按日期分组的流水列表、搜索栏、下拉加载逻辑
│   │   │   - 状态管理：负责账本/账户的切换、数据加载与刷新
│   │   │
│   │   └── record_form_sheet.dart    # 记账弹窗
│   │       - 底部弹出的记账面板
│   │       - 自定义数字键盘实现（支持四则运算逻辑）
│   │       - 包含金额输入、分类选择、日期选择、账户选择、备注输入
│   │
│   └── main.dart                     # 应用入口
│       - 配置 MaterialApp 主题（Material 3 风格）
│       - 初始化应用并挂载 HomePage
│
└── assets\
    └── finflow.db                    # 预置数据库文件
        - 应用首次启动时会自动复制此文件到本地存储目录，作为初始数据源
```

## 数据结构设计
### 交易记录表 (records)
| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER | 主键，自增 |
| bill_id | INTEGER | 关联账本 ID |
| account_id | INTEGER | 关联账户 ID |
| type | TEXT | 交易类型 (income / expense) |
| amount | REAL | 交易金额 |
| category | TEXT | 交易分类 |
| date | TEXT | 交易日期 (ISO 8601 字符串) |
| note | TEXT | 备注信息 |

### 账本表 (bills)
| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER | 主键，自增 |
| name | TEXT | 账本名称 |
| is_default | INTEGER | 是否默认账本 (0/1) |

### 账户表 (accounts)
| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER | 主键，自增 |
| name | TEXT | 账户名称 |
| is_default | INTEGER | 是否默认账户 (0/1) |

## 交互细节
1. **记账流程**：点击首页“记一笔” -> 弹出面板 -> 输入金额（支持加减） -> 选择分类/账户/日期 -> 保存。
2. **列表操作**：
   - **点击**：进入编辑模式。
   - **长按**：弹出删除确认框。
   - **搜索**：顶部搜索框输入关键字，实时过滤显示相关记录。
3. **管理功能**：
   - 在“管理”页签可切换当前账本与账户。
   - 长按账本/账户条目可进行修改名称或删除操作（默认条目除外）。
   - 删除账本时，若账本内有数据，会触发二次确认或迁移提示。
