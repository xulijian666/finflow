# 数据库字典

> **Author**: xlj
> **日期**: 2026-04-10
> **说明**: 本文档详细记录了 FinFlow 记账应用所使用的本地 SQLite 数据库表结构，涵盖账本、账户及交易记录的核心字段定义。

---

## 1. 账本表 (bills)

用于存储用户的不同账本信息（如：个人账本、家庭账本等）。

| 字段名 | 类型 | 约束 | 注释 |
| :--- | :--- | :--- | :--- |
| **id** | INTEGER | PRIMARY KEY AUTOINCREMENT | 账本唯一标识 ID |
| **name** | TEXT | NOT NULL | 账本名称 |
| **is_default** | INTEGER | NOT NULL, DEFAULT 0 | 是否为默认账本 (1: 是, 0: 否) |

---

## 2. 账户表 (accounts)

用于存储用户的不同资产账户（如：微信、支付宝、银行卡等）。

| 字段名 | 类型 | 约束 | 注释 |
| :--- | :--- | :--- | :--- |
| **id** | INTEGER | PRIMARY KEY AUTOINCREMENT | 账户唯一标识 ID |
| **name** | TEXT | NOT NULL | 账户名称 |
| **is_default** | INTEGER | NOT NULL, DEFAULT 0 | 是否为默认账户 (1: 是, 0: 否) |

---

## 3. 交易记录表 (records)

存储所有的收支流水详情。

| 字段名 | 类型 | 约束 | 注释 |
| :--- | :--- | :--- | :--- |
| **id** | INTEGER | PRIMARY KEY AUTOINCREMENT | 记录唯一标识 ID |
| **bill_id** | INTEGER | NOT NULL | 关联的账本 ID (外键逻辑) |
| **account_id** | INTEGER | NOT NULL | 关联的账户 ID (外键逻辑) |
| **type** | TEXT | NOT NULL | 交易类型 (income: 收入, expense: 支出) |
| **amount** | REAL | NOT NULL | 交易金额 |
| **category** | TEXT | NOT NULL | 交易分类 (如：餐饮、交通等) |
| **date** | TEXT | NOT NULL | 交易日期 (ISO 8601 字符串格式) |
| **note** | TEXT | - | 备注信息 |
| **quantity** | REAL | - | 数量字段 (支持小数，可为空) |

---

## 4. 基础材料表 (base_materials)

用于存储基础材料的名称、单位及初始化数量。

| 字段名 | 类型 | 约束 | 注释 |
| :--- | :--- | :--- | :--- |
| **id** | INTEGER | PRIMARY KEY AUTOINCREMENT | 基础材料唯一标识 ID |
| **name** | TEXT | NOT NULL | 材料名称 |
| **unit** | TEXT | NOT NULL | 单位 |
| **init_quantity** | REAL | NOT NULL, DEFAULT 0 | 初始化数量（库存初始化维护字段） |

> 业务约束：`name` 唯一（唯一索引约束），每个基础材料仅存在一条记录。

---

## 5. 项目-年级-课程-材料关系表 (project_material_relations)

用于记录项目、年级、课程与基础材料名称之间的关联关系。

| 字段名 | 类型 | 约束 | 注释 |
| :--- | :--- | :--- | :--- |
| **id** | INTEGER | PRIMARY KEY AUTOINCREMENT | 关系唯一标识 ID |
| **project_name** | TEXT | NOT NULL | 项目名称 |
| **grade_name** | TEXT | NOT NULL | 年级名称 |
| **course_name** | TEXT | NOT NULL | 课程名称 |
| **material_name** | TEXT | NOT NULL | 基础材料名称 |

> 唯一约束：`UNIQUE(project_name, grade_name, course_name, material_name)`

---

## 6. 材料库存记录表 (inventory_records)

用于记录材料的入库流水，与交易记录关联。

| 字段名 | 类型 | 约束 | 注释 |
| :--- | :--- | :--- | :--- |
| **id** | INTEGER | PRIMARY KEY AUTOINCREMENT | 记录唯一标识 ID |
| **material_name** | TEXT | NOT NULL | 材料名称 |
| **quantity** | REAL | NOT NULL | 数量 (支持小数) |
| **unit** | TEXT | - | 单位 |
| **record_id** | INTEGER | - | 关联的交易记录 ID (可选) |
| **created_at** | TEXT | NOT NULL | 创建时间 (ISO 8601 字符串) |

---

## 7. 材料出库记录表 (inventory_out_records)

用于记录材料出库流水，允许只记录数量与备注。

| 字段名 | 类型 | 约束 | 注释 |
| :--- | :--- | :--- | :--- |
| **id** | INTEGER | PRIMARY KEY AUTOINCREMENT | 出库记录唯一标识 ID |
| **material_name** | TEXT | NOT NULL | 出库材料名称 |
| **quantity** | REAL | NOT NULL | 出库数量 |
| **unit** | TEXT | - | 单位 |
| **note** | TEXT | - | 出库备注 |
| **created_at** | TEXT | NOT NULL | 出库时间 (ISO 8601 字符串) |

---

## 8. 材料初始化记录表 (inventory_init_records)

已移除。初始化数量统一由 `base_materials.init_quantity` 维护。
